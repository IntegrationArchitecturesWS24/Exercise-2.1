export function calculateSalesBonus(sales){
  let bonus = 0;
  if (typeof sales != Array)
    throw new Error('Sales must be an array');
  sales.forEach(element => {
    
  });


  return bonus;
}

export function calculateSocialBonus(socialPerformance){
  let bonus;

}
